---
phase: 02-spool-owner-crud
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - FilamentCatalog/wwwroot/index.html
  - FilamentCatalog/wwwroot/css/app.css
  - FilamentCatalog/wwwroot/js/api.js
autonomous: true
requirements:
  - SPOOL-01
  - SPOOL-06
  - OWNER-01
  - OWNER-02
  - BAL-01
  - BAL-02
  - BAL-03

must_haves:
  truths:
    - "index.html contains the complete page structure: sticky summary bar, filter bar, spool list, balance section (<details open>), spool dialog, owner dialog"
    - "Both modals use native <dialog> element, not div overlays"
    - "app.css declares CSS custom properties for all design tokens and contains styles for all UI components"
    - "api.js exports all 10 fetch wrapper functions covering every API endpoint"
    - "JS files use ES module syntax (export/import); index.html loads app.js with type='module'"
  artifacts:
    - path: "FilamentCatalog/wwwroot/index.html"
      provides: "Complete HTML page structure"
      contains: "<dialog id=\"spool-dialog\""
    - path: "FilamentCatalog/wwwroot/css/app.css"
      provides: "All styles using CSS custom properties"
      contains: "--color-accent: #2563eb"
    - path: "FilamentCatalog/wwwroot/js/api.js"
      provides: "Fetch wrapper module"
      contains: "export async function getSpools"
  key_links:
    - from: "FilamentCatalog/wwwroot/index.html"
      to: "FilamentCatalog/wwwroot/js/app.js"
      via: "<script type='module' src='/js/app.js'>"
      pattern: "type=\"module\""
    - from: "FilamentCatalog/wwwroot/index.html"
      to: "FilamentCatalog/wwwroot/css/app.css"
      via: "<link rel='stylesheet' href='/css/app.css'>"
      pattern: "href=\"/css/app.css\""
---

<objective>
Replace the placeholder index.html with complete page markup, create app.css with all design tokens and component styles, and create api.js with all fetch wrapper exports. This is the frontend contracts layer — feature modules (Plan 04) import from api.js and manipulate the DOM elements defined here.

Purpose: Establishes the HTML structure and JS API surface that Plan 04's feature modules depend on. Runs in Wave 1 in parallel with Plan 01 (no C# dependency).
Output: index.html (full markup), app.css (all styles), api.js (all 10 fetch wrappers).
</objective>

<execution_context>
@D:\repos\Fillament Catalog\.planning\phases\02-spool-owner-crud\execute-plan.md
@D:\repos\Fillament Catalog\.planning\phases\02-spool-owner-crud\summary.md
</execution_context>

<context>
@D:\repos\Fillament Catalog\.planning\ROADMAP.md
@D:\repos\Fillament Catalog\.planning\phases\02-spool-owner-crud\02-CONTEXT.md
@D:\repos\Fillament Catalog\.planning\phases\02-spool-owner-crud\02-UI-SPEC.md
@D:\repos\Fillament Catalog\.planning\phases\02-spool-owner-crud\02-PATTERNS.md

<interfaces>
<!-- Current placeholder index.html — will be fully replaced. -->

From FilamentCatalog/wwwroot/index.html (current — 22-line placeholder):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Filament Catalog</title>
  <style>
    /* Phase 1 placeholder styles — extend in Phase 2 */
    body { font-family: system-ui, sans-serif; margin: 0; padding: 2rem; background: #f5f5f5; }
    h1 { color: #333; }
    .status { color: #666; font-size: 0.9rem; }
  </style>
</head>
<body>
  <h1>Filament Catalog</h1>
  <main>
    <p class="status">Service is running.</p>
  </main>
</body>
</html>
```
This file is fully replaced. The inline &lt;style&gt; block is removed; styles move to /css/app.css.

<!-- Element IDs and class names that Plan 04 JS modules will query — these MUST be present in index.html exactly as specified. -->
Required IDs (Plan 04 depends on these):
- #summary-bar — sticky header element
- #stat-total-spools, #stat-my-spools, #stat-total-value, #stat-total-owed — span elements inside summary bar
- #filter-owner, #filter-material — select elements in filter bar
- #filter-search — input[type=search] in filter bar
- .chip[data-status-filter="spool"] — chip buttons for spool status (data-value="Sealed", "Active", "Empty")
- .chip[data-status-filter="payment"] — chip buttons for payment status (data-value="Paid", "Unpaid", "Partial")
- #spool-list — main element holding spool rows
- #balance-table — table inside the balance details section
- #spool-dialog — native dialog for add/edit spool
- #owner-dialog — native dialog for owner management
- #manage-owners-btn — gear button in header
- #add-spool-btn — "Add Spool" primary CTA button
- #spool-form — form inside #spool-dialog
- #spool-dialog-title — heading inside #spool-dialog
- #spool-name, #spool-material, #spool-color-picker, #spool-color-hex, #spool-color-swatch — form fields
- #spool-owner, #spool-weight, #spool-price, #spool-status, #spool-payment, #spool-notes — form fields
- #spool-save-btn, #spool-cancel-btn, #spool-delete-btn — spool form action buttons
- #spool-delete-confirm — inline confirm section (hidden by default, shown on first delete click)
- #spool-confirm-delete-btn — "Confirm Delete" button inside inline confirm section
- #spool-error — error banner element inside spool dialog
- #owner-list — container for owner rows inside #owner-dialog
- #owner-name-input, #owner-add-btn — add-owner form controls
- #owner-error — error banner element inside owner dialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html and app.css</name>
  <files>
    FilamentCatalog/wwwroot/index.html
    FilamentCatalog/wwwroot/css/app.css
  </files>
  <read_first>
    - FilamentCatalog/wwwroot/index.html — read before replacing; confirms it is safe to overwrite
    - .planning/phases/02-spool-owner-crud/02-UI-SPEC.md — authoritative source for layout, spacing, color, component specs, and copywriting contract
    - .planning/phases/02-spool-owner-crud/02-PATTERNS.md — HTML structure patterns and CSS token values
  </read_first>
  <action>
**Create FilamentCatalog/wwwroot/css/app.css** (new file — directory wwwroot/css/ must be created):

Full content:
```css
/* ---- Design tokens ---- */
:root {
    --color-bg: #f5f5f5;
    --color-surface: #ffffff;
    --color-accent: #2563eb;
    --color-destructive: #dc2626;
    --color-border: #e2e8f0;
    --color-muted: #6b7280;
    --color-warning: #f59e0b;
    --font-body: system-ui, sans-serif;
    --space-xs: 4px;
    --space-sm: 8px;
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
}

/* ---- Reset / base ---- */
*, *::before, *::after { box-sizing: border-box; }
body { margin: 0; font-family: var(--font-body); font-size: 14px; font-weight: 400; line-height: 1.5; background: var(--color-bg); color: #1f2937; }
button { font-family: inherit; cursor: pointer; }
select, input, textarea { font-family: inherit; font-size: 14px; }

/* ---- Summary bar ---- */
#summary-bar {
    position: sticky; top: 0; z-index: 100;
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
    height: 48px; padding: 0 var(--space-md);
    display: flex; align-items: center; justify-content: space-between;
}
.summary-left { display: flex; align-items: center; gap: var(--space-xl); }
.summary-title { font-size: 16px; font-weight: 600; margin: 0; }
.stat-cell { display: flex; flex-direction: column; }
.stat-label { font-size: 12px; font-weight: 600; color: var(--color-muted); text-transform: uppercase; letter-spacing: 0.05em; line-height: 1; }
.stat-value { font-size: 20px; font-weight: 600; line-height: 1.2; }
#manage-owners-btn { background: none; border: none; font-size: 18px; color: var(--color-muted); padding: var(--space-xs); border-radius: 4px; line-height: 1; }
#manage-owners-btn:hover { color: #374151; }

/* ---- Filter bar ---- */
#filter-bar {
    display: flex; flex-wrap: wrap; align-items: center; gap: var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    background: var(--color-surface); border-bottom: 1px solid var(--color-border);
}
#filter-bar select, #filter-search {
    height: 36px; border: 1px solid var(--color-border); border-radius: 6px;
    padding: 0 var(--space-sm); background: var(--color-surface); color: inherit;
}
#filter-owner, #filter-material { width: 140px; }
#filter-search { width: 200px; }
#add-spool-btn {
    margin-left: auto; height: 36px; padding: 0 var(--space-md);
    background: var(--color-accent); color: #fff; border: none; border-radius: 6px;
    font-size: 14px; font-weight: 600;
}
#add-spool-btn:hover { background: #1d4ed8; }

/* ---- Chip buttons ---- */
.chip-group { display: flex; gap: 4px; }
.chip {
    height: 32px; padding: 0 var(--space-md); border-radius: 16px;
    font-size: 14px; font-weight: 600; border: 1px solid var(--color-border);
    background: var(--color-surface); color: #374151;
}
.chip.active { background: var(--color-accent); color: #fff; border-color: var(--color-accent); }

/* ---- Spool list ---- */
#spool-list { display: flex; flex-direction: column; }
.spool-row {
    display: flex; align-items: center; gap: var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    border-bottom: 1px solid var(--color-border); background: var(--color-surface);
}
.spool-color { width: 20px; height: 20px; border-radius: 50%; border: 1px solid rgba(0,0,0,0.1); flex-shrink: 0; }
.spool-info { flex: 1; min-width: 0; }
.spool-name { font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.spool-material { color: var(--color-muted); }
.spool-owner { width: 100px; color: var(--color-muted); }
.spool-weight, .spool-price { width: 60px; }
.spool-notes-icon { color: var(--color-muted); cursor: default; }
.spool-edit-btn { margin-left: auto; background: none; border: 1px solid var(--color-border); border-radius: 4px; padding: 2px 8px; font-size: 13px; color: #374151; }
.spool-edit-btn:hover { background: var(--color-bg); }

/* Read-only status badges */
.badge {
    display: inline-block; padding: 2px 8px; border-radius: 12px;
    font-size: 12px; font-weight: 600; white-space: nowrap;
}
.badge-sealed   { background: #dbeafe; color: #1e40af; }
.badge-active   { background: #dcfce7; color: #166534; }
.badge-empty    { background: #f3f4f6; color: #6b7280; }
.badge-paid     { background: #dcfce7; color: #166534; }
.badge-unpaid   { background: #fee2e2; color: #991b1b; }
.badge-partial  { background: #fef3c7; color: #92400e; }

/* Empty state */
.empty-state { padding: var(--space-xl) var(--space-md); text-align: center; color: var(--color-muted); }
.empty-state h2 { font-size: 18px; font-weight: 600; margin: 0 0 var(--space-sm); color: #374151; }

/* ---- Balance section ---- */
#balance-section { margin: var(--space-md); border: 1px solid var(--color-border); border-radius: 8px; background: var(--color-surface); }
#balance-section summary { padding: var(--space-sm) var(--space-md); font-weight: 600; cursor: pointer; list-style: none; }
#balance-section summary::-webkit-details-marker { display: none; }
#balance-table { width: 100%; border-collapse: collapse; }
#balance-table th { padding: var(--space-sm) var(--space-md); font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: var(--color-muted); text-align: left; border-bottom: 1px solid var(--color-border); }
#balance-table td { padding: var(--space-sm) var(--space-md); border-bottom: 1px solid var(--color-border); }
#balance-table tr:last-child td { border-bottom: none; }
.warning-icon { color: var(--color-warning); }

/* ---- Modals (shared) ---- */
dialog {
    border: 1px solid var(--color-border); border-radius: 10px;
    padding: 0; box-shadow: 0 8px 32px rgba(0,0,0,0.12); max-width: 480px; width: 100%;
}
dialog::backdrop { background: rgba(0,0,0,0.4); }
.modal-header { display: flex; align-items: center; justify-content: space-between; padding: var(--space-md); border-bottom: 1px solid var(--color-border); }
.modal-title { font-size: 18px; font-weight: 600; margin: 0; }
.modal-body { padding: var(--space-md); }
.modal-footer { display: flex; align-items: center; gap: var(--space-sm); padding: var(--space-md); border-top: 1px solid var(--color-border); }

/* ---- Form fields ---- */
.form-group { display: flex; flex-direction: column; gap: var(--space-xs); margin-bottom: var(--space-sm); }
.form-group label { font-size: 13px; font-weight: 600; color: #374151; }
.form-group input[type=text], .form-group input[type=number], .form-group select, .form-group textarea {
    height: 36px; border: 1px solid var(--color-border); border-radius: 6px; padding: 0 var(--space-sm); background: var(--color-surface);
}
.form-group textarea { height: auto; padding: var(--space-sm); resize: vertical; }
.form-group input[type=text]:focus, .form-group input[type=number]:focus,
.form-group select:focus, .form-group textarea:focus {
    outline: 2px solid var(--color-accent); outline-offset: 1px;
}
.color-row { display: flex; align-items: center; gap: var(--space-sm); }
#spool-color-swatch { width: 24px; height: 24px; border-radius: 4px; border: 1px solid var(--color-border); background: #888888; }
#spool-color-hex { font-family: monospace; width: 90px; }
#spool-color-picker { width: 36px; height: 36px; padding: 2px; border: 1px solid var(--color-border); border-radius: 6px; cursor: pointer; background: none; }

/* ---- Buttons ---- */
.btn-primary { height: 36px; padding: 0 var(--space-md); background: var(--color-accent); color: #fff; border: none; border-radius: 6px; font-size: 14px; font-weight: 600; }
.btn-primary:hover { background: #1d4ed8; }
.btn-secondary { height: 36px; padding: 0 var(--space-md); background: var(--color-surface); color: #374151; border: 1px solid var(--color-border); border-radius: 6px; font-size: 14px; }
.btn-secondary:hover { background: var(--color-bg); }
.btn-destructive { height: 36px; padding: 0 var(--space-md); background: var(--color-destructive); color: #fff; border: none; border-radius: 6px; font-size: 14px; font-weight: 600; }
.btn-destructive:hover { background: #b91c1c; }
.btn-destructive-outline { height: 36px; padding: 0 var(--space-md); background: none; color: var(--color-destructive); border: 1px solid var(--color-destructive); border-radius: 6px; font-size: 14px; }

/* ---- Delete confirm (inline in spool dialog footer) ---- */
#spool-delete-confirm { display: flex; align-items: center; gap: var(--space-sm); }
#spool-delete-confirm p { margin: 0; font-size: 13px; color: #374151; }

/* ---- Error banner ---- */
.error-banner {
    background: #fee2e2; border: 1px solid #fca5a5; color: #991b1b;
    padding: var(--space-sm) var(--space-md); border-radius: 6px; font-size: 14px; margin-top: var(--space-sm);
}

/* ---- Owner modal ---- */
#owner-dialog { max-width: 400px; }
.owner-row { display: flex; align-items: center; gap: var(--space-sm); padding: var(--space-sm) 0; border-bottom: 1px solid var(--color-border); }
.owner-name { flex: 1; font-weight: 600; }
.owner-me-badge { font-size: 12px; color: var(--color-muted); font-weight: 400; }
.owner-add-form { display: flex; gap: var(--space-sm); margin-top: var(--space-md); }
.owner-add-form input { flex: 1; height: 36px; border: 1px solid var(--color-border); border-radius: 6px; padding: 0 var(--space-sm); }
```

**Replace FilamentCatalog/wwwroot/index.html** (complete replacement — remove placeholder inline style block):

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Filament Catalog</title>
  <link rel="stylesheet" href="/css/app.css" />
</head>
<body>

  <!-- Sticky summary bar -->
  <header id="summary-bar">
    <div class="summary-left">
      <h1 class="summary-title">Filament Catalog</h1>
      <div class="stat-cell">
        <span class="stat-label">Total Spools</span>
        <span class="stat-value" id="stat-total-spools">—</span>
      </div>
      <div class="stat-cell">
        <span class="stat-label">My Spools</span>
        <span class="stat-value" id="stat-my-spools">—</span>
      </div>
      <div class="stat-cell">
        <span class="stat-label">Total Value</span>
        <span class="stat-value" id="stat-total-value">—</span>
      </div>
      <div class="stat-cell">
        <span class="stat-label">Total Owed</span>
        <span class="stat-value" id="stat-total-owed">—</span>
      </div>
    </div>
    <button id="manage-owners-btn" aria-label="Manage owners" title="Manage owners">⚙</button>
  </header>

  <!-- Filter bar -->
  <div id="filter-bar">
    <select id="filter-owner">
      <option value="">All owners</option>
    </select>
    <select id="filter-material">
      <option value="">All materials</option>
    </select>
    <div class="chip-group" id="chip-group-spool">
      <button type="button" class="chip" data-status-filter="spool" data-value="Sealed">Sealed</button>
      <button type="button" class="chip" data-status-filter="spool" data-value="Active">Active</button>
      <button type="button" class="chip" data-status-filter="spool" data-value="Empty">Empty</button>
    </div>
    <div class="chip-group" id="chip-group-payment">
      <button type="button" class="chip" data-status-filter="payment" data-value="Paid">Paid</button>
      <button type="button" class="chip" data-status-filter="payment" data-value="Unpaid">Unpaid</button>
      <button type="button" class="chip" data-status-filter="payment" data-value="Partial">Partial</button>
    </div>
    <input type="search" id="filter-search" placeholder="Search name, material, notes…" />
    <button type="button" id="add-spool-btn">Add Spool</button>
  </div>

  <!-- Spool list -->
  <main id="spool-list"></main>

  <!-- Balance section -->
  <details open id="balance-section">
    <summary>Balance Overview</summary>
    <table id="balance-table">
      <thead>
        <tr>
          <th>Owner</th>
          <th>Spools</th>
          <th>Value</th>
          <th>Owed</th>
        </tr>
      </thead>
      <tbody></tbody>
    </table>
  </details>

  <!-- Spool add/edit dialog -->
  <dialog id="spool-dialog">
    <div class="modal-header">
      <h2 class="modal-title" id="spool-dialog-title">Add Spool</h2>
    </div>
    <div class="modal-body">
      <div id="spool-error" class="error-banner" style="display:none"></div>
      <form id="spool-form" novalidate>
        <div class="form-group">
          <label for="spool-name">Name <span aria-hidden="true">*</span></label>
          <input type="text" id="spool-name" required placeholder="e.g. Bambu PLA Basic" />
        </div>
        <div class="form-group">
          <label for="spool-material">Material <span aria-hidden="true">*</span></label>
          <input type="text" id="spool-material" required placeholder="e.g. PLA" />
        </div>
        <div class="form-group">
          <label>Color</label>
          <div class="color-row">
            <input type="color" id="spool-color-picker" value="#888888" />
            <input type="text" id="spool-color-hex" maxlength="7" pattern="^#[0-9A-Fa-f]{6}$" placeholder="#888888" value="#888888" />
            <div id="spool-color-swatch"></div>
          </div>
        </div>
        <div class="form-group">
          <label for="spool-owner">Owner <span aria-hidden="true">*</span></label>
          <select id="spool-owner" required></select>
        </div>
        <div class="form-group">
          <label for="spool-weight">Weight (g)</label>
          <input type="number" id="spool-weight" min="1" placeholder="e.g. 1000" />
        </div>
        <div class="form-group">
          <label for="spool-price">Price paid (€)</label>
          <input type="number" id="spool-price" min="0" step="0.01" placeholder="e.g. 24.99" />
        </div>
        <div class="form-group">
          <label for="spool-status">Spool status</label>
          <select id="spool-status">
            <option value="Sealed">Sealed</option>
            <option value="Active">Active</option>
            <option value="Empty">Empty</option>
          </select>
        </div>
        <div class="form-group">
          <label for="spool-payment">Payment status</label>
          <select id="spool-payment">
            <option value="Unpaid">Unpaid</option>
            <option value="Paid">Paid</option>
            <option value="Partial">Partial</option>
          </select>
        </div>
        <div class="form-group">
          <label for="spool-notes">Notes</label>
          <textarea id="spool-notes" rows="2" placeholder="Optional notes"></textarea>
        </div>
      </form>
    </div>
    <div class="modal-footer">
      <button type="button" id="spool-delete-btn" class="btn-destructive-outline" style="display:none">Delete Spool</button>
      <div id="spool-delete-confirm" style="display:none">
        <p>Are you sure? This cannot be undone.</p>
        <button type="button" id="spool-confirm-delete-btn" class="btn-destructive">Confirm Delete</button>
      </div>
      <button type="button" id="spool-cancel-btn" class="btn-secondary" style="margin-left:auto">Discard Changes</button>
      <button type="button" id="spool-save-btn" class="btn-primary">Save Spool</button>
    </div>
  </dialog>

  <!-- Owner management dialog -->
  <dialog id="owner-dialog">
    <div class="modal-header">
      <h2 class="modal-title">Manage Owners</h2>
    </div>
    <div class="modal-body">
      <div id="owner-list"></div>
      <div id="owner-error" class="error-banner" style="display:none"></div>
      <div class="owner-add-form">
        <input type="text" id="owner-name-input" placeholder="Owner name" />
        <button type="button" id="owner-add-btn" class="btn-primary">Add Owner</button>
      </div>
    </div>
  </dialog>

  <script type="module" src="/js/app.js"></script>
</body>
</html>
```

Key structural rules enforced:
- `<details open>` for balance section — no JS toggle (D-05)
- `<dialog>` native elements for both modals — not div overlays (CLAUDE.md)
- `<script type="module">` — ES modules, no bundler (CLAUDE.md)
- Single `<link rel="stylesheet">` — no inline style block in the replaced page
- Gear icon uses Unicode ⚙ with aria-label="Manage owners" (UI-SPEC copywriting contract)
- Delete confirm is inline in footer, not a separate dialog (UI-SPEC)
- novalidate on form — JS handles validation via API response errors
  </action>
  <verify>
    <automated>
      grep -n "dialog id=\"spool-dialog\"" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "dialog id=\"owner-dialog\"" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "details open" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "type=\"module\"" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "stat-total-spools" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "stat-my-spools" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "stat-total-value" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "stat-total-owed" "FilamentCatalog/wwwroot/index.html" &amp;&amp;
      grep -n "\-\-color-accent: #2563eb" "FilamentCatalog/wwwroot/css/app.css" &amp;&amp;
      grep -n "badge-unpaid" "FilamentCatalog/wwwroot/css/app.css"
    </automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<dialog id="spool-dialog"` and `<dialog id="owner-dialog"` (native dialog, not div)
    - index.html contains `<details open id="balance-section">` (starts expanded, D-05)
    - index.html contains `<script type="module" src="/js/app.js">` (ES module entry point)
    - index.html contains `<link rel="stylesheet" href="/css/app.css">` and NO inline `<style>` block
    - index.html contains all stat IDs: `id="stat-total-spools"`, `id="stat-my-spools"`, `id="stat-total-value"`, `id="stat-total-owed"`
    - index.html contains all required form field IDs: `id="spool-name"`, `id="spool-material"`, `id="spool-color-picker"`, `id="spool-color-hex"`, `id="spool-owner"`, `id="spool-weight"`, `id="spool-price"`, `id="spool-status"`, `id="spool-payment"`, `id="spool-notes"`
    - index.html contains `id="spool-delete-btn"` and `id="spool-delete-confirm"` and `id="spool-confirm-delete-btn"`
    - index.html contains `id="owner-list"`, `id="owner-name-input"`, `id="owner-add-btn"`
    - index.html contains `id="filter-owner"`, `id="filter-material"`, `id="filter-search"`, `id="add-spool-btn"`
    - index.html contains chip buttons with `data-status-filter="spool"` and `data-status-filter="payment"` attributes
    - app.css contains `--color-accent: #2563eb`, `--color-destructive: #dc2626`, `--color-warning: #f59e0b`
    - app.css contains `.badge-sealed`, `.badge-active`, `.badge-empty`, `.badge-paid`, `.badge-unpaid`, `.badge-partial` classes
    - app.css contains `#summary-bar` with `position: sticky` and `z-index: 100`
    - No `<style>` tag remains inside index.html (all styles in app.css)
  </acceptance_criteria>
  <done>index.html has complete page structure with all required IDs; app.css has all design tokens and component styles; placeholder inline styles removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create api.js with all fetch wrappers</name>
  <files>FilamentCatalog/wwwroot/js/api.js</files>
  <read_first>
    - .planning/phases/02-spool-owner-crud/02-PATTERNS.md — api.js fetch wrapper pattern (Pattern 4 in RESEARCH.md)
    - .planning/phases/02-spool-owner-crud/02-CONTEXT.md — D-08 lists all 9 endpoints (10 wrapper functions needed: GET spools is one call)
  </read_first>
  <action>
Create FilamentCatalog/wwwroot/js/api.js (directory wwwroot/js/ must be created). This is an ES module — all functions are named exports. No default export.

```javascript
// api.js — fetch wrappers for all Filament Catalog API endpoints
// All functions are async and throw Error on non-2xx responses.

// ---- Owners ----

export async function getOwners() {
    const res = await fetch('/api/owners');
    if (!res.ok) throw new Error(`GET /api/owners failed: ${res.status}`);
    return res.json();
}

export async function createOwner(name) {
    const res = await fetch('/api/owners', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name })
    });
    if (!res.ok) {
        const data = await res.json().catch(() => ({}));
        throw new Error(data.error ?? `POST /api/owners failed: ${res.status}`);
    }
    return res.json();
}

export async function deleteOwner(id) {
    const res = await fetch(`/api/owners/${id}`, { method: 'DELETE' });
    if (!res.ok) {
        const data = await res.json().catch(() => ({}));
        throw new Error(data.error ?? `DELETE /api/owners/${id} failed: ${res.status}`);
    }
    // 204 No Content — no body to parse
}

// ---- Spools ----

export async function getSpools() {
    const res = await fetch('/api/spools');
    if (!res.ok) throw new Error(`GET /api/spools failed: ${res.status}`);
    return res.json();
}

export async function createSpool(data) {
    const res = await fetch('/api/spools', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
    });
    if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw new Error(body.error ?? `POST /api/spools failed: ${res.status}`);
    }
    return res.json();
}

export async function updateSpool(id, data) {
    const res = await fetch(`/api/spools/${id}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
    });
    if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw new Error(body.error ?? `PUT /api/spools/${id} failed: ${res.status}`);
    }
    return res.json();
}

export async function deleteSpool(id) {
    const res = await fetch(`/api/spools/${id}`, { method: 'DELETE' });
    if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw new Error(body.error ?? `DELETE /api/spools/${id} failed: ${res.status}`);
    }
    // 204 No Content — no body to parse
}

// ---- Summary ----

export async function getSummary() {
    const res = await fetch('/api/summary');
    if (!res.ok) throw new Error(`GET /api/summary failed: ${res.status}`);
    return res.json();
}

// ---- Balance ----

export async function getBalance() {
    const res = await fetch('/api/balance');
    if (!res.ok) throw new Error(`GET /api/balance failed: ${res.status}`);
    return res.json();
}
```

Functions exported: `getOwners`, `createOwner`, `deleteOwner`, `getSpools`, `createSpool`, `updateSpool`, `deleteSpool`, `getSummary`, `getBalance` — 9 functions covering all 9 API endpoints defined in D-08.

Error handling: All mutation functions parse `{ error: "..." }` from the response body and re-throw as Error with the server message. This surfaces the server's error text directly in the UI (Plan 04 catches it and displays in the error banner).
  </action>
  <verify>
    <automated>
      grep -n "export async function getOwners" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function createOwner" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function deleteOwner" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function getSpools" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function createSpool" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function updateSpool" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function deleteSpool" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function getSummary" "FilamentCatalog/wwwroot/js/api.js" &amp;&amp;
      grep -n "export async function getBalance" "FilamentCatalog/wwwroot/js/api.js"
    </automated>
  </verify>
  <acceptance_criteria>
    - api.js contains exactly 9 exported async functions: `getOwners`, `createOwner`, `deleteOwner`, `getSpools`, `createSpool`, `updateSpool`, `deleteSpool`, `getSummary`, `getBalance`
    - Mutation functions (createOwner, createSpool, updateSpool) include `headers: { 'Content-Type': 'application/json' }` and `body: JSON.stringify(data)`
    - Delete functions send `method: 'DELETE'` and do NOT attempt to parse the 204 response body
    - Error handling parses `data.error` from JSON response body before throwing
    - No `import` statements (api.js is the lowest-level module — it imports nothing)
    - File uses ES module syntax (export keyword on each function, no CommonJS module.exports)
  </acceptance_criteria>
  <done>api.js exists with all 9 exported async fetch wrappers; error handling extracts server error message; no imports (leaf module).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → HTML form fields | All data from form fields is untrusted until sent to the API |
| API response → innerHTML | Any response data injected into the DOM could cause XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Injection (XSS) | summary.js renderBalance, spools.js render | mitigate | Plan 04 must use `textContent` not `innerHTML` when rendering API response strings (owner names, spool names). The balance table render pattern in PATTERNS.md uses template literals with innerHTML — Plan 04 MUST sanitize or switch to textContent/createElement for user-supplied strings. Note this in Plan 04 task actions. |
| T-02-03-02 | Injection (XSS) | owner-dialog owner name display | mitigate | Plan 04 must set `element.textContent = owner.name` — not innerHTML interpolation |
| T-02-03-03 | Spoofing | api.js fetch calls | accept | localhost-only app; no CSRF risk from external origins since the API has no auth and is not exposed outside the machine |
| T-02-03-04 | Information Disclosure | index.html served at localhost:5000 | accept | No secrets in HTML; CSS/JS contains no credentials; single-user local app by design |
</threat_model>

<verification>
After both tasks complete:
1. Navigate to http://localhost:5000 — page loads (summary bar, filter bar, empty spool list, balance section visible)
2. Balance section starts expanded (not collapsed) — `<details open>` working
3. Gear icon visible in header; clicking it opens the owner modal
4. "Add Spool" button visible in filter bar
5. Browser console: no errors about missing module imports or missing IDs
6. `grep -c "export async function" FilamentCatalog/wwwroot/js/api.js` returns 9
</verification>

<success_criteria>
- http://localhost:5000 serves the new index.html (not the placeholder) with no 404s on css/js resources
- Both modals open with `.showModal()` (native dialog behavior) — no div overlays
- Balance section is visible and expanded by default
- All required element IDs are present in the DOM
- api.js has 9 exported fetch wrappers
- No inline &lt;style&gt; block in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/02-spool-owner-crud/02-03-SUMMARY.md` following the summary template.
</output>
