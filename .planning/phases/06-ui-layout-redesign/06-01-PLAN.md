---
phase: 06-ui-layout-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FilamentCatalog.Service/wwwroot/index.html
  - FilamentCatalog.Service/wwwroot/css/app.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Page has a two-column layout: 240px balance sidebar on the left, spool column on the right"
    - "Sidebar is sticky at top: 48px and does not move when the spool list grows"
    - "Balance table renders inside <aside id='balance-sidebar'> — no <details> wrapper"
    - "Filter bar is inside #spool-column, not at body level"
    - "Expand/Collapse All button is present in the filter bar"
  artifacts:
    - path: "FilamentCatalog.Service/wwwroot/index.html"
      provides: "Two-column layout wrapper, balance sidebar aside, spool column div"
      contains: "id=\"main-layout\""
    - path: "FilamentCatalog.Service/wwwroot/index.html"
      provides: "Expand/Collapse All button in filter bar"
      contains: "id=\"expand-collapse-btn\""
    - path: "FilamentCatalog.Service/wwwroot/css/app.css"
      provides: "Layout, sidebar, owner-group CSS rules"
      contains: "#balance-sidebar"
  key_links:
    - from: "#summary-bar"
      to: "#main-layout"
      via: "sibling elements — main-layout comes immediately after summary-bar in body"
      pattern: "summary-bar.*main-layout"
    - from: "#balance-sidebar"
      to: "#balance-table"
      via: "direct child — aside contains table, no details wrapper"
      pattern: "balance-sidebar.*balance-table"
---

<objective>
Restructure index.html into a two-column layout and add all CSS rules required by the design contract.

Purpose: Move the page from a single-column stacked layout to a sidebar + spool-column layout. The balance table moves into a sticky aside. The filter bar moves inside the spool column. The spool list remains a <main> element but now sits in the right column.

Output:
- index.html with #main-layout wrapper, #balance-sidebar aside, #spool-column div, #expand-collapse-btn button
- app.css with all new layout/component rules added; #balance-section rules removed
</objective>

<execution_context>
@D:/repos/Fillament Catalog/.claude/get-shit-done/workflows/execute-plan.md
@D:/repos/Fillament Catalog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/repos/Fillament Catalog/.planning/PROJECT.md
@D:/repos/Fillament Catalog/.planning/ROADMAP.md
@D:/repos/Fillament Catalog/.planning/STATE.md

<interfaces>
<!-- Existing design tokens from app.css :root (executor must reference these, not hard-code values) -->
--color-bg: #f5f5f5
--color-surface: #ffffff
--color-accent: #2563eb
--color-border: #e2e8f0
--color-muted: #6b7280
--space-xs: 4px
--space-sm: 8px
--space-md: 16px
--space-lg: 24px
--space-xl: 32px

<!-- Existing CSS rules the executor must NOT remove or rename -->
#balance-table, #balance-table th, #balance-table td — keep all three rules, only remove #balance-section and #balance-section summary
.badge — keep, it is reused by owner-spool-count

<!-- IDs referenced by spools.js (must not be renamed) -->
id="spool-list"     — <main> element, rendered into by spools.js
id="filter-owner"   — select element, referenced by spools.js filterOwner
id="filter-material"
id="filter-search"
id="add-spool-btn"
id="balance-table"  — rendered into by summary.js renderBalance

<!-- New IDs specified by decisions -->
id="main-layout"        — D-01: two-column flex wrapper
id="balance-sidebar"    — D-05: replaces <details id="balance-section">
id="spool-column"       — D-01: right column
id="expand-collapse-btn" — D-09: collapse/expand all button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure index.html into two-column layout</name>
  <files>FilamentCatalog.Service/wwwroot/index.html</files>
  <read_first>
    - FilamentCatalog.Service/wwwroot/index.html — read the full current structure before any edit
    - .planning/phases/06-ui-layout-redesign/06-UI-SPEC.md — authoritative DOM structure spec
  </read_first>
  <action>
Replace the three elements currently sitting after #summary-bar (the filter bar div, the spool list main, and the balance-section details) with the following structure. The dialogs and script tag stay at the end of body — unchanged.

REMOVE these three elements:
1. `<div id="filter-bar">…</div>`
2. `<main id="spool-list"></main>`
3. `<details open id="balance-section">…</details>`

REPLACE with:

```html
  <div id="main-layout">
    <aside id="balance-sidebar">
      <h2>Balance Overview</h2>
      <table id="balance-table">
        <thead>
          <tr>
            <th>Owner</th>
            <th>Spools</th>
            <th>Value</th>
            <th>Owed</th>
          </tr>
        </thead>
        <tbody></tbody>
      </table>
    </aside>
    <div id="spool-column">
      <div id="filter-bar">
        <select id="filter-owner">
          <option value="">All owners</option>
        </select>
        <select id="filter-material">
          <option value="">All materials</option>
        </select>
        <div class="chip-group" id="chip-group-spool">
          <button type="button" class="chip" data-status-filter="spool" data-value="Sealed">Sealed</button>
          <button type="button" class="chip" data-status-filter="spool" data-value="Active">Active</button>
          <button type="button" class="chip" data-status-filter="spool" data-value="Empty">Empty</button>
        </div>
        <div class="chip-group" id="chip-group-payment">
          <button type="button" class="chip" data-status-filter="payment" data-value="Paid">Paid</button>
          <button type="button" class="chip" data-status-filter="payment" data-value="Unpaid">Unpaid</button>
          <button type="button" class="chip" data-status-filter="payment" data-value="Partial">Partial</button>
        </div>
        <input type="search" id="filter-search" placeholder="Search name, material, notes&#8230;" />
        <p id="catalog-empty-notice" class="info-banner" hidden>Sync the Bambu catalog first to enable adding spools.</p>
        <button type="button" id="expand-collapse-btn" class="btn-secondary">Collapse all</button>
        <button type="button" id="add-spool-btn">Add Spool</button>
      </div>
      <main id="spool-list"></main>
    </div>
  </div>
```

Key decisions honored:
- per D-01: two-column #main-layout; sidebar left, spool column right
- per D-02: #filter-bar is inside #spool-column only
- per D-04: sidebar contains only the balance table; header stats unchanged
- per D-05: <aside id="balance-sidebar"> — no <details> wrapper; always visible
- per D-09: #expand-collapse-btn with class btn-secondary, label "Collapse all", placed immediately before #add-spool-btn
- Sidebar heading uses <h2>Balance Overview</h2> — plain element, not inside <summary>
- All existing IDs preserved: filter-owner, filter-material, filter-search, add-spool-btn, balance-table, spool-list, catalog-empty-notice
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('FilamentCatalog.Service/wwwroot/index.html','utf8');const checks=['id=\"main-layout\"','id=\"balance-sidebar\"','id=\"spool-column\"','id=\"expand-collapse-btn\"','Balance Overview','id=\"balance-table\"','id=\"filter-bar\"','id=\"spool-list\"','id=\"add-spool-btn\"'];const missing=checks.filter(c=>!h.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}const removed=['details','balance-section','summary>Balance'];const present=removed.filter(c=>h.includes(c));if(present.length){console.error('SHOULD BE REMOVED (still present):',present);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains id="main-layout"
    - index.html contains id="balance-sidebar"
    - index.html contains id="spool-column"
    - index.html contains id="expand-collapse-btn"
    - index.html contains id="balance-table" inside #balance-sidebar (not inside details)
    - index.html contains id="filter-bar" as a child of #spool-column
    - index.html contains id="spool-list" as a child of #spool-column
    - index.html does NOT contain `<details` (the old balance-section details element is removed)
    - index.html does NOT contain `id="balance-section"` (old ID gone)
    - All existing dialog elements (#spool-dialog, #owner-dialog) and script tag unchanged
  </acceptance_criteria>
  <done>index.html renders a two-column layout structure with sidebar and spool column; all JS-referenced IDs preserved; old details balance section removed</done>
</task>

<task type="auto">
  <name>Task 2: Add layout and tree-view CSS rules; remove obsolete balance-section rules</name>
  <files>FilamentCatalog.Service/wwwroot/css/app.css</files>
  <read_first>
    - FilamentCatalog.Service/wwwroot/css/app.css — read full file before editing; understand existing rules to avoid duplication
    - .planning/phases/06-ui-layout-redesign/06-UI-SPEC.md — authoritative CSS values
  </read_first>
  <action>
Make the following targeted changes to app.css:

**REMOVE** the two #balance-section rules (lines ~99-102 in current file):
```css
#balance-section { margin: var(--space-md); border: 1px solid var(--color-border); border-radius: 8px; background: var(--color-surface); }
#balance-section summary { padding: var(--space-sm) var(--space-md); font-weight: 600; cursor: pointer; list-style: none; }
#balance-section summary::-webkit-details-marker { display: none; }
```
Keep all three `#balance-table` rules intact — they are unchanged.

**ADD** the following block immediately after the `/* ---- Balance section ---- */` comment area (after the retained #balance-table rules), replacing the removed rules with new sidebar and layout rules. Label the section `/* ---- Phase 6: Layout redesign ---- */`:

```css
/* ---- Phase 6: Layout redesign ---- */

/* Two-column wrapper */
#main-layout {
    display: flex;
    align-items: flex-start;
}

/* Balance sidebar */
#balance-sidebar {
    width: 240px;
    flex-shrink: 0;
    position: sticky;
    top: 48px;
    align-self: flex-start;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    margin: var(--space-md);
}
#balance-sidebar h2 {
    font-size: 14px;
    font-weight: 600;
    margin: 0;
    padding: var(--space-sm) var(--space-md);
    border-bottom: 1px solid var(--color-border);
}

/* Spool column (right side) */
#spool-column {
    flex: 1;
    min-width: 0;
    overflow-y: auto;
}

/* Owner group tree view */
.owner-group-header {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-border);
    cursor: pointer;
}
.owner-chevron {
    color: var(--color-muted);
    font-size: 12px;
}
.owner-group-name {
    font-size: 14px;
    font-weight: 600;
    flex: 1;
}
.owner-spool-count {
    background: var(--color-bg);
    color: var(--color-muted);
    border: 1px solid var(--color-border);
}
.owner-group-rows .spool-row {
    padding-left: 32px;
}
```

Key decisions honored:
- per D-01/D-03: #main-layout flex, #balance-sidebar sticky top: 48px
- per D-05: #balance-sidebar width: 240px
- per D-06: .owner-group-header background: var(--color-bg), cursor: pointer
- per D-06: .owner-chevron color: var(--color-muted), font-size: 12px
- per D-06: .owner-group-name font-weight: 600, flex: 1
- .owner-spool-count modifier: bg var(--color-bg), muted text, border — used alongside .badge
- .owner-group-rows .spool-row: padding-left: 32px for visual nesting
- DO NOT add `display: none` to .owner-group-rows — the JS in Plan 02 uses the `hidden` attribute for collapse; CSS handles `.spool-row[hidden]` already via existing rule
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('FilamentCatalog.Service/wwwroot/css/app.css','utf8');const required=['#main-layout','#balance-sidebar','#spool-column','.owner-group-header','.owner-chevron','.owner-group-name','.owner-spool-count','.owner-group-rows .spool-row','#balance-table'];const missing=required.filter(r=>!css.includes(r));if(missing.length){console.error('MISSING CSS:',missing);process.exit(1);}const removed=['#balance-section {','#balance-section summary {','#balance-section summary::-webkit'];const present=removed.filter(r=>css.includes(r));if(present.length){console.error('SHOULD BE REMOVED (still in CSS):',present);process.exit(1);}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains #main-layout with display: flex
    - app.css contains #balance-sidebar with width: 240px
    - app.css contains #balance-sidebar with position: sticky
    - app.css contains #balance-sidebar with top: 48px
    - app.css contains #spool-column with flex: 1
    - app.css contains .owner-group-header with background: var(--color-bg)
    - app.css contains .owner-chevron with color: var(--color-muted)
    - app.css contains .owner-group-name with font-weight: 600
    - app.css contains .owner-spool-count with color: var(--color-muted)
    - app.css contains .owner-group-rows .spool-row with padding-left: 32px
    - app.css does NOT contain `#balance-section {` (old rule removed)
    - app.css does NOT contain `#balance-section summary` (old rule removed)
    - app.css still contains `#balance-table` (unchanged rule preserved)
    - app.css still contains `#balance-table th` (unchanged rule preserved)
  </acceptance_criteria>
  <done>app.css has all Phase 6 layout and tree-view component rules; obsolete #balance-section rules removed; all existing rules intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file serving | index.html and app.css are static assets served by ASP.NET Core; no user input in these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | index.html restructure | accept | Pure structural HTML; no user data, no script changes in this plan; no new attack surface |
| T-06-02 | Information Disclosure | CSS layout rules | accept | CSS is public; design tokens already exposed; no secrets in layout rules |
</threat_model>

<verification>
After both tasks complete:
1. Open index.html in a text editor and confirm #main-layout wraps #balance-sidebar and #spool-column
2. Confirm #balance-table is a direct child of #balance-sidebar (no details wrapper)
3. Confirm #expand-collapse-btn exists in #filter-bar inside #spool-column
4. Confirm app.css has no #balance-section rules and has #balance-sidebar rules
5. The page will not render correctly until Plan 02 updates spools.js (tree view JS), but the HTML/CSS structure must be complete
</verification>

<success_criteria>
- index.html contains #main-layout, #balance-sidebar, #spool-column, #expand-collapse-btn
- app.css contains all new layout and tree-view CSS rules
- Old <details id="balance-section"> removed from HTML and its CSS rules removed from app.css
- All IDs referenced by existing JS modules (spools.js, summary.js, app.js) remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-layout-redesign/06-01-SUMMARY.md`
</output>
