---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FilamentCatalog/FilamentCatalog.csproj
  - FilamentCatalog/appsettings.json
  - FilamentCatalog/Models/Owner.cs
  - FilamentCatalog/AppDbContext.cs
  - FilamentCatalog/wwwroot/index.html
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "The project builds with `dotnet build` using Microsoft.NET.Sdk.Web"
    - "The Owner entity has Id, Name (required), IsMe, CreatedAt properties"
    - "AppDbContext exposes an Owners DbSet and accepts DbContextOptions"
    - "wwwroot/index.html exists and contains a structural HTML skeleton"
    - "appsettings.json binds Kestrel to http://localhost:5000"
  artifacts:
    - path: "FilamentCatalog/FilamentCatalog.csproj"
      provides: "Project definition with all required NuGet packages"
      contains: "Microsoft.NET.Sdk.Web"
    - path: "FilamentCatalog/Models/Owner.cs"
      provides: "Owner entity for EF Core"
      exports: ["Owner"]
    - path: "FilamentCatalog/AppDbContext.cs"
      provides: "EF Core DbContext with Owners DbSet"
      exports: ["AppDbContext"]
    - path: "FilamentCatalog/wwwroot/index.html"
      provides: "Placeholder page served by UseDefaultFiles + UseStaticFiles"
      contains: "<!DOCTYPE html>"
    - path: "FilamentCatalog/appsettings.json"
      provides: "Kestrel URL binding"
      contains: "http://localhost:5000"
  key_links:
    - from: "FilamentCatalog/AppDbContext.cs"
      to: "FilamentCatalog/Models/Owner.cs"
      via: "DbSet<Owner>"
      pattern: "DbSet<Owner>"
---

<objective>
Scaffold the FilamentCatalog .NET 10 project with all source files needed before Program.cs and EF migrations can be created.

Purpose: Plan 02 cannot write Program.cs or run `dotnet ef migrations add` until the csproj (with correct SDK and packages), the Owner model, and AppDbContext all exist. This plan creates those foundations.
Output: Compilable project skeleton — csproj, configuration, data model, DbContext, and placeholder static page. No executable behavior yet; that is Plan 02's job.
</objective>

<execution_context>
@D:/repos/Fillament Catalog/.claude/get-shit-done/workflows/execute-plan.md
@D:/repos/Fillament Catalog/.planning/templates/summary.md
</execution_context>

<context>
@D:/repos/Fillament Catalog/.planning/ROADMAP.md
@D:/repos/Fillament Catalog/CLAUDE.md
@D:/repos/Fillament Catalog/.planning/phases/01-foundation/01-RESEARCH.md
@D:/repos/Fillament Catalog/.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold (csproj, appsettings.json, wwwroot/index.html)</name>
  <read_first>
    - D:/repos/Fillament Catalog/CLAUDE.md — conventions, hard rules
    - D:/repos/Fillament Catalog/.planning/phases/01-foundation/01-PATTERNS.md — verbatim file patterns for csproj, appsettings.json, wwwroot/index.html
    - D:/repos/Fillament Catalog/.planning/phases/01-foundation/01-RESEARCH.md — Standard Stack section for package versions
  </read_first>
  <action>
Create the directory structure and three files verbatim from the patterns in 01-PATTERNS.md:

1. Create directory `FilamentCatalog/wwwroot/` (needed before static files can be served).

2. Create `FilamentCatalog/FilamentCatalog.csproj` with EXACTLY this content (no deviations):
```xml
<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsTransformWebConfigDisabled>true</IsTransformWebConfigDisabled>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting.WindowsServices" Version="10.0.7" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.7" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.7">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
  </ItemGroup>
</Project>
```
SDK MUST be `Microsoft.NET.Sdk.Web` — not Worker. `IsTransformWebConfigDisabled` suppresses spurious web.config generation. Design package gets PrivateAssets.

3. Create `FilamentCatalog/appsettings.json` with EXACTLY this content:
```json
{
  "Urls": "http://localhost:5000",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
```

4. Create `FilamentCatalog/wwwroot/index.html` as a structural HTML skeleton (per D-01 — Claude's discretion: balance between minimal and structural). Use this content:
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Filament Catalog</title>
  <style>
    /* Phase 1 placeholder styles — extend in Phase 2 */
    body { font-family: system-ui, sans-serif; margin: 0; padding: 2rem; background: #f5f5f5; }
    h1 { color: #333; }
    .status { color: #666; font-size: 0.9rem; }
  </style>
</head>
<body>
  <h1>Filament Catalog</h1>
  <!-- Phase 2: replace this placeholder with actual UI -->
  <main>
    <p class="status">Service is running.</p>
  </main>
  <!-- Phase 2: add ES module scripts here -->
</body>
</html>
```

Do NOT use `MapStaticAssets` — per RESEARCH.md, it does not support default document serving. `UseDefaultFiles` + `UseStaticFiles` is the correct pair and will be wired in Program.cs (Plan 02).
  </action>
  <verify>
    <automated>
      cd /d/repos/Fillament\ Catalog/FilamentCatalog &amp;&amp;
      grep -c 'Microsoft.NET.Sdk.Web' FilamentCatalog.csproj &amp;&amp;
      grep -c 'IsTransformWebConfigDisabled' FilamentCatalog.csproj &amp;&amp;
      grep -c 'localhost:5000' appsettings.json &amp;&amp;
      grep -c 'DOCTYPE html' wwwroot/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `FilamentCatalog/FilamentCatalog.csproj` exists and contains `Sdk="Microsoft.NET.Sdk.Web"`
    - csproj contains `&lt;IsTransformWebConfigDisabled&gt;true&lt;/IsTransformWebConfigDisabled&gt;`
    - csproj contains `Microsoft.Extensions.Hosting.WindowsServices` Version="10.0.7"
    - csproj contains `Microsoft.EntityFrameworkCore.Sqlite` Version="10.0.7"
    - csproj contains `Microsoft.EntityFrameworkCore.Design` Version="10.0.7" with `&lt;PrivateAssets&gt;all&lt;/PrivateAssets&gt;`
    - csproj contains `Serilog.AspNetCore` Version="10.0.0"
    - `FilamentCatalog/appsettings.json` contains `"Urls": "http://localhost:5000"`
    - `FilamentCatalog/wwwroot/index.html` exists and begins with `&lt;!DOCTYPE html&gt;`
    - wwwroot/index.html contains `&lt;h1&gt;Filament Catalog&lt;/h1&gt;` and `Service is running`
  </acceptance_criteria>
  <done>Project directory exists with csproj (correct SDK + 4 packages), appsettings.json (localhost:5000), and wwwroot/index.html (structural skeleton).</done>
</task>

<task type="auto">
  <name>Task 2: Create Owner model and AppDbContext</name>
  <read_first>
    - D:/repos/Fillament Catalog/CLAUDE.md — DateTime.UtcNow hard rule
    - D:/repos/Fillament Catalog/.planning/phases/01-foundation/01-PATTERNS.md — verbatim patterns for Models/Owner.cs and AppDbContext.cs
  </read_first>
  <action>
Create two files verbatim from the patterns in 01-PATTERNS.md:

1. Create `FilamentCatalog/Models/Owner.cs`:
```csharp
public class Owner
{
    public int Id { get; set; }
    public required string Name { get; set; }
    public bool IsMe { get; set; }
    public DateTime CreatedAt { get; set; }
}
```
Rules:
- `Name` uses `required` keyword (C# 11+, matches `&lt;Nullable&gt;enable&lt;/Nullable&gt;` in csproj)
- `CreatedAt` is `DateTime` — NEVER `DateTimeOffset`. Values are set with `DateTime.UtcNow` at write time (CLAUDE.md hard rule)
- No navigation properties — Phase 2+ adds those via their own migrations
- No `[Key]` attribute needed — EF Core convention maps `Id` as primary key automatically

2. Create `FilamentCatalog/AppDbContext.cs`:
```csharp
using Microsoft.EntityFrameworkCore;

public class AppDbContext : DbContext
{
    public AppDbContext(DbContextOptions&lt;AppDbContext&gt; options) : base(options) { }

    public DbSet&lt;Owner&gt; Owners =&gt; Set&lt;Owner&gt;();
}
```
Rules:
- Phase 1 contains ONLY the `Owners` DbSet — do NOT add Spool or BambuProduct sets here
- `Set&lt;Owner&gt;()` pattern (not `{ get; set; }`) matches the canonical Phase 1 pattern
- No `OnModelCreating` override needed in Phase 1 — EF Core conventions are sufficient

After creating both files, run `dotnet build FilamentCatalog/FilamentCatalog.csproj` (from the repo root) to verify the project compiles. If it fails, read the error and fix before marking done.
  </action>
  <verify>
    <automated>
      cd /d/repos/Fillament\ Catalog &amp;&amp;
      grep -c 'required string Name' FilamentCatalog/Models/Owner.cs &amp;&amp;
      grep -c 'DateTime CreatedAt' FilamentCatalog/Models/Owner.cs &amp;&amp;
      grep -c 'DbSet&lt;Owner&gt;' FilamentCatalog/AppDbContext.cs &amp;&amp;
      dotnet build FilamentCatalog/FilamentCatalog.csproj --nologo -q 2&gt;&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `FilamentCatalog/Models/Owner.cs` exists and contains `public required string Name`
    - Owner.cs contains `public DateTime CreatedAt` (not DateTimeOffset, not DateOnly)
    - Owner.cs does NOT contain `DateTime.Now` anywhere (only assigned at runtime in Program.cs with UtcNow)
    - `FilamentCatalog/AppDbContext.cs` exists and contains `DbSet&lt;Owner&gt; Owners`
    - AppDbContext.cs does NOT contain DbSet entries for Spool or BambuProduct (Phase 2/3 only)
    - `dotnet build FilamentCatalog/FilamentCatalog.csproj` exits with code 0 (no errors)
  </acceptance_criteria>
  <done>Owner model and AppDbContext compile cleanly. `dotnet build` exits 0. The EF Core model is ready for `dotnet ef migrations add` in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem | App writes filament.db and logs/ next to executable — no network boundary in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | filament.db file | accept | Local-only app; single user; no authentication scope in v1 — physical access assumed trusted |
| T-01-02 | Denial of Service | Log file growth | mitigate | `retainedFileCountLimit: 7` in Serilog sink caps log disk usage to ~7 daily files (wired in Plan 02) |
| T-01-03 | Information Disclosure | appsettings.json | accept | No secrets in Phase 1 config; only Kestrel URL and log levels |
</threat_model>

<verification>
After both tasks complete:
1. `dotnet build FilamentCatalog/FilamentCatalog.csproj` exits 0
2. `FilamentCatalog/Models/Owner.cs` — grep confirms `required string Name`, `DateTime CreatedAt`, no `DateTime.Now`
3. `FilamentCatalog/AppDbContext.cs` — grep confirms `DbSet&lt;Owner&gt;`, no Spool/BambuProduct sets
4. `FilamentCatalog/wwwroot/index.html` exists with DOCTYPE and "Filament Catalog" title
5. `FilamentCatalog/appsettings.json` contains `"Urls": "http://localhost:5000"`
6. `FilamentCatalog/FilamentCatalog.csproj` uses `Microsoft.NET.Sdk.Web`, has 4 packages at correct versions
</verification>

<success_criteria>
- Project compiles without errors or warnings
- Owner model matches the canonical pattern exactly (required Name, bool IsMe, DateTime CreatedAt)
- AppDbContext has exactly one DbSet (Owners) — no Phase 2+ leakage
- All CLAUDE.md conventions honored: no DateTime.Now, no relative paths in source (paths wired in Plan 02)
- wwwroot/index.html is a structural skeleton Phase 2 can extend (not a throwaway hello world)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
